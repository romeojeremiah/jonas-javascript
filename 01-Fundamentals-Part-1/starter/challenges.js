// let markMass = 78;
// let johnMass = 92;
// let markHeight = 1.69;
// let johnHeight = 1.95;

// let bmiMark = markMass / markHeight ** 2;
// let bmiJohn = johnMass / johnHeight ** 2;
// let markHigherBMI = bmiMark > bmiJohn;

// console.log(bmiMark, bmiJohn, markHigherBMI);

// markMass = 95;
// johnMass = 85;
// markHeight = 1.88;
// johnHeight = 1.76;

// bmiMark = markMass / markHeight ** 2;
// bmiJohn = johnMass / johnHeight ** 2;
// markHigherBMI = bmiMark > bmiJohn;

// console.log(bmiMark, bmiJohn, markHigherBMI);

// CHALLENGE #4

// if (markHigherBMI) {
//     console.log(
//         `Mark's BMI is higher than John's! Mark's BMI is ${bmiMark.toFixed(2)}.`
//     );
// } else {
//     console.log(`John's BMI is higher than Mark's!
//   John's BMI is ${bmiJohn.toFixed(2)}.`);
// }

// CHALLENGE #3

// const dolphinsAvg = (97 + 112 + 80) / 3;
// const koalasAvg = (109 + 95 + 50) / 3;
// console.log(dolphinsAvg, koalasAvg);

// if (dolphinsAvg > koalasAvg && dolphinsAvg > 100) {
//     console.log("winner", "Dolphins");
// } else if (koalasAvg > dolphinsAvg && koalasAvg > 100) {
//     console.log("winner", "Koalas");
// } else if (dolphinsAvg === koalasAvg && koalasAvg > 100 && dolphinsAvg > 100) {
//     console.log("draw");
// } else {
//     console.log(
//         `No winner, both average scores < 100. Dolphins ${dolphinsAvg.toFixed(
//             2
//         )}, Koalas ${koalasAvg.toFixed(2)}`
//     );
// }

// CHALLENGE #4
// let bill = 275;

// let tip = bill >= 50 && bill <= 300 ? bill * 0.15 : bill * 0.2;
// console.log(`The bill was ${bill}, the tip was ${tip},
// and the total value is ${bill + tip}`);

// bill = 40;

// tip = bill >= 50 && bill <= 300 ? bill * 0.15 : bill * 0.2;
// console.log(`The bill was ${bill}, the tip was ${tip},
// and the total value is ${bill + tip}`);

// bill = 430;
// tip = bill >= 50 && bill <= 300 ? bill * 0.15 : bill * 0.2;
// console.log(`The bill was ${bill}, the tip was ${tip},
// and the total value is ${bill + tip}`);
